[project]
name = "tools"
version = "0.1.0"
description = "Browser-based utilities"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "playwright",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
